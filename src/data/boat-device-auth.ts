import "server-only";
import { createHash, randomBytes, randomUUID, timingSafeEqual } from "node:crypto";
import postgres from "postgres";

export type BoatPermission="snapshot:read"|"operating-log:write";
export type BoatDevicePrincipal={deviceId:string;assetId:string;ownerId:string;permissions:BoatPermission[]};
let client:ReturnType<typeof postgres>|undefined;
function database(){const u=process.env.DATABASE_URL;if(!u)throw new Error("DATABASE_URL is required");client??=postgres(u,{max:5});return client}
function hashSecret(secret:string){return createHash("sha256").update(secret,"utf8").digest("hex")}
function parseCredential(value:string|null){if(!value)return null;const match=/^BoatDevice\s+([^\s]+)$/i.exec(value.trim());if(!match)return null;const raw=match[1],dot=raw.indexOf(".");if(dot<1)return null;return{deviceId:raw.slice(0,dot),secret:raw.slice(dot+1)}}
export function createBoatDeviceSecret(){const deviceId=randomUUID(),secret=randomBytes(32).toString("base64url");return{deviceId,secret,credential:`BoatDevice ${deviceId}.${secret}`,secretHash:hashSecret(secret)}}
export async function enrollBoatDevice({assetId,ownerId,label,permissions=["snapshot:read"]}:{assetId:string;ownerId:string;label:string;permissions?:BoatPermission[]}){const sql=database(),generated=createBoatDeviceSecret();const owned=await sql<{id:string}[]>`SELECT id FROM assets WHERE id=${assetId} AND owner_id=${ownerId} LIMIT 1`;if(!owned[0])throw new Error("Asset not found or not owned by the current user.");await sql`INSERT INTO boat_devices(id,asset_id,owner_id,label,secret_hash,permissions) VALUES(${generated.deviceId}::uuid,${assetId},${ownerId},${label.trim()||"Onboard Ernest"},${generated.secretHash},${permissions})`;return{deviceId:generated.deviceId,credential:generated.credential,permissions}}
export async function revokeBoatDevice(deviceId:string,ownerId:string){const sql=database(),rows=await sql<{id:string}[]>`UPDATE boat_devices SET revoked_at=now() WHERE id=${deviceId}::uuid AND owner_id=${ownerId} AND revoked_at IS NULL RETURNING id`;return Boolean(rows[0])}
export async function authenticateBoatDevice(authorization:string|null,expectedAssetId:string,required:BoatPermission):Promise<BoatDevicePrincipal|null>{const parsed=parseCredential(authorization);if(!parsed)return null;let sql;try{sql=database()}catch{return null}const rows=await sql<{id:string;asset_id:string;owner_id:string;secret_hash:string;permissions:string[];revoked_at:Date|null}[]>`SELECT id,asset_id,owner_id,secret_hash,permissions,revoked_at FROM boat_devices WHERE id=${parsed.deviceId}::uuid LIMIT 1`;const row=rows[0];if(!row||row.revoked_at||row.asset_id!==expectedAssetId)return null;const supplied=Buffer.from(hashSecret(parsed.secret),"hex"),stored=Buffer.from(row.secret_hash,"hex");if(supplied.length!==stored.length||!timingSafeEqual(supplied,stored))return null;const permissions=row.permissions as BoatPermission[];if(!permissions.includes(required))return null;void sql`UPDATE boat_devices SET last_used_at=now() WHERE id=${row.id}::uuid`.catch(()=>{});return{deviceId:row.id,assetId:row.asset_id,ownerId:row.owner_id,permissions}}
