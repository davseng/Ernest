"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";

import { askErnest, type AskErnestState } from "@/app/assets/[id]/ask-actions";
import { saveConversationMessage, saveConversationMessageState, startConversation } from "@/app/assets/[id]/conversation-actions";
import { confirmErnestWrite } from "@/app/assets/[id]/ernest-write-actions";
import type { ErnestWriteProposal } from "@/data/ernest-write-proposals";

type ChatMessage={id:string;role:"user"|"assistant";text:string;sources?:AskErnestState["sources"];proposal?:ErnestWriteProposal;writeResult?:{ok:boolean;message:string}};
type RecentConversation={id:string;title:string;updatedAt:string};
const emptyState:AskErnestState={question:"",answer:"",sources:[]};

function ProposalDetails({proposal}:{proposal:ErnestWriteProposal}){
  if(proposal.kind==="log")return <dl className="write-proposal-details"><div><dt>Type</dt><dd>{proposal.log.entryType}</dd></div><div><dt>Date</dt><dd>{proposal.log.occurredAt}</dd></div><div><dt>Title</dt><dd>{proposal.log.title}</dd></div><div className="write-proposal-wide"><dt>Entry</dt><dd>{proposal.log.body}</dd></div></dl>;
  if(proposal.kind==="component_fact")return <dl className="write-proposal-details"><div><dt>Equipment</dt><dd>{proposal.componentFact.componentName}</dd></div><div><dt>Field</dt><dd>{proposal.componentFact.field}</dd></div><div className="write-proposal-wide"><dt>New value</dt><dd>{proposal.componentFact.value}</dd></div></dl>;
  if(proposal.kind==="component_lifecycle")return <dl className="write-proposal-details"><div><dt>Equipment</dt><dd>{proposal.lifecycle.componentName}</dd></div><div><dt>Status</dt><dd>{proposal.lifecycle.status==="removed_replaced"?"Removed / Replaced":proposal.lifecycle.status==="unknown"?"Unknown":"Installed"}</dd></div>{proposal.lifecycle.changedOn?<div><dt>Date</dt><dd>{proposal.lifecycle.changedOn}</dd></div>:null}{proposal.lifecycle.notes?<div className="write-proposal-wide"><dt>Lifecycle note</dt><dd>{proposal.lifecycle.notes}</dd></div>:null}</dl>;
  if(proposal.kind==="inventory_add")return <dl className="write-proposal-details"><div><dt>Inventory item</dt><dd>{proposal.inventory.name}</dd></div><div><dt>Location</dt><dd>{proposal.inventory.locationCode||"Not specified"}</dd></div>{proposal.inventory.quantity?<div><dt>Quantity</dt><dd>{proposal.inventory.quantity}</dd></div>:null}{proposal.inventory.details?<div className="write-proposal-wide"><dt>Details</dt><dd>{proposal.inventory.details}</dd></div>:null}</dl>;
  if(proposal.kind==="inventory_update")return <dl className="write-proposal-details"><div><dt>Inventory item</dt><dd>{proposal.inventory.currentName}</dd></div>{proposal.inventory.name?<div><dt>New name</dt><dd>{proposal.inventory.name}</dd></div>:null}{proposal.inventory.locationCode?<div><dt>Location</dt><dd>{proposal.inventory.locationCode}</dd></div>:null}{proposal.inventory.quantity?<div><dt>Quantity</dt><dd>{proposal.inventory.quantity}</dd></div>:null}{proposal.inventory.details?<div className="write-proposal-wide"><dt>Details</dt><dd>{proposal.inventory.details}</dd></div>:null}</dl>;
  if(proposal.kind==="procedure_add"||proposal.kind==="procedure_update")return <div className="write-proposal-procedure"><dl className="write-proposal-details"><div><dt>{proposal.kind==="procedure_add"?"New procedure":"Procedure"}</dt><dd>{proposal.kind==="procedure_add"?proposal.procedure.title:proposal.procedure.currentTitle}</dd></div><div><dt>Type</dt><dd>{proposal.procedure.procedureType}</dd></div>{proposal.procedure.title!==(("currentTitle" in proposal.procedure&&proposal.procedure.currentTitle)||proposal.procedure.title)?<div><dt>New title</dt><dd>{proposal.procedure.title}</dd></div>:null}</dl><p><strong>Complete resulting steps</strong></p><ol>{proposal.procedure.steps.map((step,index)=><li key={`${index}-${step.instruction}`}>{step.instruction}{step.note?` — ${step.note}`:""}</li>)}</ol>{proposal.procedure.notes?<p className="muted">Notes: {proposal.procedure.notes}</p>:null}</div>;
  return <dl className="write-proposal-details"><div><dt>Asset field</dt><dd>{proposal.assetFact.field}</dd></div><div className="write-proposal-wide"><dt>Value</dt><dd>{proposal.assetFact.value}</dd></div></dl>;
}
function proposalHeading(proposal:ErnestWriteProposal){if(proposal.kind.startsWith("inventory_"))return"Proposed inventory change";if(proposal.kind==="log")return"Proposed log entry";if(proposal.kind.startsWith("procedure_"))return"Proposed procedure change";if(proposal.kind==="component_lifecycle"||proposal.kind==="component_fact")return"Proposed equipment change";return"Proposed verified fact";}
function conversationContext(messages:ChatMessage[]){return messages.slice(-10).map(m=>`${m.role==="user"?"Owner":"Ernest"}: ${m.text}`).join("\n").slice(-6000);}
function autoTitle(question:string){const compact=question.replace(/\s+/g," ").trim();return compact.length<=60?compact:`${compact.slice(0,57).trimEnd()}…`;}

export function ErnestChat({assetId,assetName,initialConversationId,initialMessages,recentConversations}:{assetId:string;assetName:string;initialConversationId?:string;initialMessages:ChatMessage[];recentConversations:RecentConversation[]}){
  const[conversationId,setConversationId]=useState<string|undefined>(initialConversationId);const[messages,setMessages]=useState<ChatMessage[]>(initialMessages);const[recents,setRecents]=useState(recentConversations);const[input,setInput]=useState("");const[pending,setPending]=useState(false);const[savingId,setSavingId]=useState<string|null>(null);const[thinkHarder,setThinkHarder]=useState(false);const[thinkingHarderNow,setThinkingHarderNow]=useState(false);
  async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const question=input.trim();if(!question||pending)return;const useThinkHarder=thinkHarder,prior=conversationContext(messages),tempUserId=crypto.randomUUID();setMessages(c=>[...c,{id:tempUserId,role:"user",text:question}]);setInput("");setThinkHarder(false);setThinkingHarderNow(useThinkHarder);setPending(true);try{let activeConversationId=conversationId;if(!activeConversationId){const started=await startConversation(assetId,question);if(!started.ok)throw new Error(started.error);activeConversationId=started.conversation.id;setConversationId(activeConversationId);setRecents(c=>[{id:started.conversation.id,title:started.conversation.title,updatedAt:new Date().toISOString()},...c.filter(x=>x.id!==started.conversation.id)]);const url=new URL(window.location.href);url.searchParams.set("asset",assetId);url.searchParams.set("chat",activeConversationId);window.history.replaceState(null,"",url.toString());}else setRecents(c=>c.map(x=>x.id===activeConversationId?{...x,updatedAt:new Date().toISOString()}:x).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)));
    const savedUser=await saveConversationMessage(assetId,activeConversationId,{role:"user",text:question});if(savedUser.ok)setMessages(c=>c.map(m=>m.id===tempUserId?{...m,id:savedUser.messageId}:m));
    const formData=new FormData();formData.set("question",question);formData.set("conversation",prior);formData.set("thinkHarder",useThinkHarder?"true":"false");const result=await askErnest(assetId,emptyState,formData);const assistantText=result.error||result.answer||"I couldn’t answer that right now.";const savedAssistant=await saveConversationMessage(assetId,activeConversationId,{role:"assistant",text:assistantText,sources:result.sources,proposal:result.proposal});setMessages(c=>[...c,{id:savedAssistant.ok?savedAssistant.messageId:crypto.randomUUID(),role:"assistant",text:assistantText,sources:result.sources,proposal:result.proposal}]);if(!conversationId){const title=autoTitle(question);setRecents(c=>c.map(x=>x.id===activeConversationId?{...x,title}:x));}}
    catch{setMessages(c=>[...c,{id:crypto.randomUUID(),role:"assistant",text:"I couldn’t save or continue that conversation right now. Please try again."}]);}finally{setPending(false);setThinkingHarderNow(false);}}
  async function saveProposal(messageId:string,proposal:ErnestWriteProposal){if(savingId)return;setSavingId(messageId);try{const result=await confirmErnestWrite(assetId,proposal),nextProposal=result.ok?undefined:proposal;setMessages(c=>c.map(m=>m.id===messageId?{...m,proposal:nextProposal,writeResult:result}:m));if(conversationId)await saveConversationMessageState(assetId,conversationId,messageId,{proposal:nextProposal,writeResult:result});}catch{const writeResult={ok:false,message:"I couldn’t save that. Please try again."};setMessages(c=>c.map(m=>m.id===messageId?{...m,writeResult}:m));}finally{setSavingId(null);}}
  async function dismissProposal(messageId:string){const writeResult={ok:true,message:"Not saved."};setMessages(c=>c.map(m=>m.id===messageId?{...m,proposal:undefined,writeResult}:m));if(conversationId)await saveConversationMessageState(assetId,conversationId,messageId,{writeResult});}
  function newChat(){if(pending||savingId)return;if(messages.some(m=>m.proposal)&&!window.confirm("Start a new chat? Any unconfirmed proposal will stay in this conversation so you can return to it later."))return;setConversationId(undefined);setMessages([]);setInput("");setThinkHarder(false);const url=new URL(window.location.href);url.searchParams.set("asset",assetId);url.searchParams.delete("chat");window.history.replaceState(null,"",url.toString());}
  const recentPanel=<div className="recent-chats-panel"><strong>Recent chats</strong>{recents.length?recents.map(c=><Link key={c.id} href={`/?asset=${assetId}&chat=${c.id}`}><span>{c.title}</span><small>{new Date(c.updatedAt).toLocaleDateString()}</small></Link>):<p>No saved chats yet.</p>}</div>;
  return <section className="ernest-chat" aria-label={`Conversation about ${assetName}`}><div className="ernest-chat-scroll">{messages.length===0?<div className="ernest-welcome"><div className="ernest-welcome-toolbar"><details className="recent-chats-menu"><summary>Recent chats</summary>{recentPanel}</details></div><div className="ernest-orb" aria-hidden="true">E</div><h1>What do you need from Ernest?</h1><p className="ernest-context">Working with <strong>{assetName}</strong></p><p>Ask a question, run an onboard task, or tell Ernest something that changed.</p><div className="ernest-operate-actions"><Link href={`/assets/${assetId}/procedures#checklists`}>✓ <span>Checklist</span></Link><Link className="emergency-action" href={`/assets/${assetId}/procedures#emergency`}>! <span>Emergency</span></Link><button type="button" onClick={()=>setInput("I want to record an update: ")}>＋ <span>Update</span></button></div><div className="ernest-prompts"><button type="button" onClick={()=>setInput("Where can I find ")}>Find something</button><button type="button" onClick={()=>setInput("Update this equipment: ")}>Update equipment</button><button type="button" onClick={()=>setInput("Change my checklist: ")}>Change checklist</button></div></div>:<div className="ernest-thread"><div className="chat-thread-toolbar"><details className="recent-chats-menu"><summary>Recent chats</summary>{recentPanel}</details><button type="button" onClick={newChat} disabled={pending||!!savingId}>＋ New chat</button></div>{messages.map(message=><article className={`chat-message ${message.role}`} key={message.id}><div className="chat-role">{message.role==="assistant"?"Ernest":"You"}</div><div className="chat-bubble"><p>{message.text}</p></div>{message.sources?.length?<div className="chat-sources">{message.sources.map(s=><span key={`${s.documentTitle}-${s.pageNumber}`}>✓ {s.documentTitle} · p. {s.pageNumber}</span>)}</div>:null}{message.proposal?<div className="write-proposal"><div className="write-proposal-heading"><span>{proposalHeading(message.proposal)}</span><strong>Nothing is saved until you confirm.</strong></div><p>{message.proposal.summary}</p><ProposalDetails proposal={message.proposal}/><div className="write-proposal-actions"><button className="write-confirm" type="button" disabled={savingId===message.id} onClick={()=>saveProposal(message.id,message.proposal!)}>{savingId===message.id?"Saving…":"Confirm & save"}</button><button className="write-cancel" type="button" onClick={()=>dismissProposal(message.id)}>Don’t save</button></div></div>:null}{message.writeResult?<p className={message.writeResult.ok?"write-result success":"write-result error"}>{message.writeResult.message}</p>:null}</article>)}{pending?<article className="chat-message assistant"><div className="chat-role">Ernest</div><div className="chat-bubble thinking"><span>{thinkingHarderNow?"Thinking harder":"Thinking"}</span><span>…</span></div></article>:null}</div>}</div><div className="ernest-composer-wrap"><div className="ernest-composer-controls"><button className={thinkHarder?"think-harder-toggle active":"think-harder-toggle"} type="button" aria-pressed={thinkHarder} disabled={pending} onClick={()=>setThinkHarder(c=>!c)}>✦ Think harder</button>{thinkHarder?<span>For the next message only</span>:null}</div><form className="ernest-composer" onSubmit={submit}><textarea aria-label={`Ask or tell Ernest about ${assetName}`} value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();e.currentTarget.form?.requestSubmit();}}} placeholder="Ask or tell Ernest anything…" rows={1} maxLength={500}/><button type="submit" disabled={pending||!input.trim()} aria-label="Send message">↑</button></form><p className="ernest-composer-note">Ernest can read your trusted knowledge. Changes require confirmation.</p></div></section>;
}
